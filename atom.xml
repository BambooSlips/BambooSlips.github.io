<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Hexo</title>
  
  
  <link href="http://example.com/atom.xml" rel="self"/>
  
  <link href="http://example.com/"/>
  <updated>2022-03-09T08:20:54.215Z</updated>
  <id>http://example.com/</id>
  
  <author>
    <name>John Doe</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>软件测试 Software Testing</title>
    <link href="http://example.com/2021/05/27/TestScroll/"/>
    <id>http://example.com/2021/05/27/TestScroll/</id>
    <published>2021-05-27T12:39:00.000Z</published>
    <updated>2022-03-09T08:20:54.215Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/images/st1.png" alt="st1"></p><h2 id="软件测试的概念"><a href="#软件测试的概念" class="headerlink" title="软件测试的概念"></a>软件测试的概念</h2><h3 id="软件"><a href="#软件" class="headerlink" title="软件"></a>软件</h3><p>软件=程序+文档</p><h3 id="软件测试"><a href="#软件测试" class="headerlink" title="软件测试"></a>软件测试</h3><p>软件测试（Software Testing），描述一种用来促进鉴定软件的正确性、完整性、安全性和质量的过程。换句话说，软件测试是一种实际输出与预期输出之间的审核或者比较过程。软件测试的经典定义是：在规定的条件下对程序进行操作，以发现程序错误，衡量软件质量，并对其是否能满足设计要求进行评估的过程。</p><h3 id="缺陷"><a href="#缺陷" class="headerlink" title="缺陷"></a>缺陷</h3><h4 id="什么是软件缺陷"><a href="#什么是软件缺陷" class="headerlink" title="什么是软件缺陷"></a>什么是软件缺陷</h4><ul><li>没有实现《需求规格说明书》中需要实现的功能</li><li>实现了《需求规格说明书》中没有提到的功能</li><li>实现了基本功能，但未实现一些虽未明确规定但应该实现的目标，如软件难以理解，不易使用，运行缓慢等</li></ul><h4 id="产生原因"><a href="#产生原因" class="headerlink" title="产生原因"></a>产生原因</h4><ul><li>软件产品说明书（需求）</li><li>设计</li><li>编写代码</li><li>其它</li></ul><h3 id="测试用例"><a href="#测试用例" class="headerlink" title="测试用例"></a>测试用例</h3><ul><li>定义：在测试执行之前设计的一套详细的测试方案，包括测试环境、测试步骤、测试数据和预期结果</li><li>测试用例=输入+输出+测试环境<ul><li>输入：测试数据和操作步骤</li><li>输出：期望结果</li><li>测试环境：系统环境设置</li></ul></li><li>设计测试用例是创造性劳动，而执行测试用例是体力劳动，设计测试用例是软件测试的关键</li></ul><h2 id="黑盒测试"><a href="#黑盒测试" class="headerlink" title="黑盒测试"></a>黑盒测试</h2><h3 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h3><ul><li><p>又叫功能测试，数据驱动测试或基于规格说明的测试。（实际上，黑盒测试包括功能测试和性能测试）</p></li><li><p>测试过程</p><pre class="mermaid">  graph LRA[需求说明] --&gt; |产生| B[被测程序] --&gt;|输出| C[测试结果]C[测试结果] --&gt;|比较| A[需求说明]</pre></li><li><p>黑盒测试的难点在于如何构造有效的输入</p><ul><li>由于输入空间通常是无限的，穷举测试显然行不通。 </li><li>寻找最小最重要的用例集合以精简测试复杂性。</li></ul></li></ul><h3 id="除因果图外各种方法"><a href="#除因果图外各种方法" class="headerlink" title="除因果图外各种方法"></a>除因果图外各种方法</h3><h4 id="边界值分析"><a href="#边界值分析" class="headerlink" title="边界值分析"></a>边界值分析</h4><h5 id="基本原理"><a href="#基本原理" class="headerlink" title="基本原理"></a>基本原理</h5><h5 id="基本思想"><a href="#基本思想" class="headerlink" title="基本思想"></a>基本思想</h5><ul><li>在最小值、略高于最小值、正常值、略低于最大值和最大值处取输入变量值 </li><li>表示方法min、min+、nom、max-和max</li></ul><h5 id="边界值分析获得测试用例的方法"><a href="#边界值分析获得测试用例的方法" class="headerlink" title="边界值分析获得测试用例的方法"></a>边界值分析获得测试用例的方法</h5><ul><li>使所有变量取正常值，只使一个变量取极值； </li><li>对于一个n变量函数，边界值分析会产生4n+1个测试用例。</li></ul><h5 id="两个变量函数边界值分析测试用例"><a href="#两个变量函数边界值分析测试用例" class="headerlink" title="两个变量函数边界值分析测试用例"></a>两个变量函数边界值分析测试用例</h5><ul><li>X1取值：x1min,x1min+,x1nom,x1max-,x1max </li><li>X2取值：x2min,x2min+ ,x2nom,x2max-,x2max</li></ul><h5 id="健壮性测试"><a href="#健壮性测试" class="headerlink" title="健壮性测试"></a>健壮性测试</h5><h4 id="基于判定表的测试"><a href="#基于判定表的测试" class="headerlink" title="基于判定表的测试"></a>基于判定表的测试</h4><h5 id="判定表的组成"><a href="#判定表的组成" class="headerlink" title="判定表的组成"></a>判定表的组成</h5><ul><li>条件桩（Condition Stub）<br>列出问题的所有条件 </li><li>动作桩（Action Stub）<br>列出可能采取的操作 </li><li>条件项（Condition Entity）<br>列出条件桩的取值 </li><li>动作项（Action Entity）<br>列出条件项各种取值下应该采取的动作</li></ul><h5 id="判定表的组成-1"><a href="#判定表的组成-1" class="headerlink" title="判定表的组成"></a>判定表的组成</h5><h5 id="规则合并"><a href="#规则合并" class="headerlink" title="规则合并"></a>规则合并</h5><ul><li>规则合并就是判定表的简化。 </li><li>有两条或多条规则具有相同的动作，并且其条件项之间存在着极为相似的关系，就可以将规则合并。</li></ul><h5 id="判定表建立步骤"><a href="#判定表建立步骤" class="headerlink" title="判定表建立步骤"></a>判定表建立步骤</h5><ul><li>根据软件规格说明<br>① 列出所有的条件桩和动作桩；<br>② 确定规则的个数；<br> 假如有n个条件，每个条件有两个取值（0,1）,则有2^n 种规则；<br>③ 填入条件项；<br>④ 填入动作项，得到初始决策表；<br>⑤ 简化，合并相似规则（相同动作）。</li></ul><h5 id="判定表的优点"><a href="#判定表的优点" class="headerlink" title="判定表的优点"></a>判定表的优点</h5><ul><li>能把复杂的问题按各种可能的情况一一列举出来 </li><li>简明而易于理解 </li><li>可避免遗漏</li></ul><h5 id="判定表的缺点"><a href="#判定表的缺点" class="headerlink" title="判定表的缺点"></a>判定表的缺点</h5><ul><li><p>不能表达重复执行的动作，例如循环结构 </p></li><li><p>判定表不能很好地伸缩 </p></li><li><p>有n个条件的决策表有2^n 个规则</p></li></ul><h4 id="场景法"><a href="#场景法" class="headerlink" title="场景法"></a>场景法</h4><h3 id="划分等价类"><a href="#划分等价类" class="headerlink" title="划分等价类"></a>划分等价类</h3><h4 id="等价类定义"><a href="#等价类定义" class="headerlink" title="等价类定义"></a>等价类定义</h4><p>等价类是输入域的某个子集合，而所有的等价类的并集是整个输入域。在子集合中，各个输入数据对于揭露程序中的错误是等效的。</p><h4 id="有效等价类"><a href="#有效等价类" class="headerlink" title="有效等价类"></a>有效等价类</h4><p>对于程序的规格说明来说，是合理的、有意义的输入数据所构成的集合；</p><h4 id="无效等价类"><a href="#无效等价类" class="headerlink" title="无效等价类"></a>无效等价类</h4><p>对于程序的规格说明来说，是不合理的、没有意义的输入数据所构成的集合</p><h4 id="确定等价类的原则"><a href="#确定等价类的原则" class="headerlink" title="确定等价类的原则"></a>确定等价类的原则</h4><ul><li>如果输入条件规定了取值范围或值的个数就可确定一个有效等价类和两个无效等价类</li><li>如果输入条件是一个布尔量,则可以确立一个有效等价类和一个无效等价类。</li><li>如果规定了输入数据的一组值, 且程序要对每一个输入值分别进行处理, 要对每一个规定的输入值确立一个有效等价类,而对于这组值之外的所有值确立一个无效等价类。</li><li>如果规定了输入数据必须遵循的规则，可确定一个有效等价类和若干个无效等价类（从不同角度违反规则)。 （其他情况）</li><li>如已划分的等价类各元素在程序中的处理方式不同，则应将此等价类进一步划分成更小的等价类</li></ul><h4 id="用等价类设计测试用例"><a href="#用等价类设计测试用例" class="headerlink" title="用等价类设计测试用例"></a>用等价类设计测试用例</h4><ul><li>分析输入输出，划分等价类，形成等价类表； </li><li>设计一新的测试用例，使其尽可能多地覆盖尚未覆盖的有效等价类，重复这一步骤，直到所有的有效等价类都被覆盖为止； </li><li>设计一新测试用例，使其只覆盖一个无效等价类，重复这一步骤直到所有无效等价类均被覆盖；</li></ul><h3 id="写测试用例"><a href="#写测试用例" class="headerlink" title="写测试用例"></a>写测试用例</h3><h2 id="Web测试"><a href="#Web测试" class="headerlink" title="Web测试"></a>Web测试</h2><h3 id="概念-1"><a href="#概念-1" class="headerlink" title="概念"></a>概念</h3><blockquote><p>如：搜索功能如何测试（角度）</p></blockquote><h4 id="表单测试"><a href="#表单测试" class="headerlink" title="表单测试"></a>表单测试</h4><ul><li>测试方法<ul><li>边界值测试</li><li>等价类测试</li><li>异常类测试</li></ul></li><li>文本框测试常见验证点<ul><li>输入正常的字母或数字，验证是否能正常工作。</li><li>输入已存在的用户名或电子邮件名称，验证是否有唯一性校验。</li><li>输入超长字符串，例如在“名称”框中输入超过允许边界个数的字符，假设最多255字符，尝试输入256个或以上字符，检查程序能否正确处理。</li><li>输入默认值，空白，空格，检查程序能否正确处理。</li><li>若只允许输入字母，尝试输入数字；反之；尝试输入字母，检查程序能否正确处理。</li><li>利用复制，粘贴等操作强制输入程序不允许的输入数据，检查程序能否正确处理。</li><li>输入特殊字符集，例如，NULL及\n等，检查程序能否正确处理。</li><li>输入中文、英文、数字、特殊字符（特别注意单引号和反斜杠）及这四类的混合输入，检查程序能否正确处理。</li><li>输入不符合格式的数据，检查程序是否正常校验，如：程序要求输入身份证号，</li><li>输入abc123，程序应该给出错误提示。</li><li>输入html语言的&lt;head&gt;、&lt;html&gt;、&lt;b&gt;等，检查是否能原样正确显示。</li><li>输入全角、半角的英文、数字、特殊字符等，检查是否报错。</li></ul></li><li>特殊输入域常见验证点<ul><li>密码框测试常见验证点<ul><li>密码输入域输入数据是否可见？密码的正确显示必须为“******”，不可见模式。</li><li>密码是否可以全部是空格？密码设计必须不能全为空格。</li><li>密码是否对大小写敏感？比如：密码“An@d123R”与“an@d123r”，一定不是同一个密码。</li></ul></li><li>日期填充域常见验证点 <ul><li>输入不符合格式的数据，检查程序是否正常校验，如：程序要求输入年月日格式为yy/mm/dd，实际输入yyyy/mm/dd，程序应该给出错误提示。</li><li>无效日期处理，如：出生年月输入为2013/02/30，我们知道2月最多是29天，检查程序是否出错。</li><li>出生日期填写为未来日期，比如3214/12/12，检查程序是否出错。</li><li>将结束日期设置在开始日期之前，检查是否有正常校验。</li></ul></li><li>电话号码填充域常见验证点<ul><li>电话号码应该由一组数字组成，不能包含英文字母。</li><li>如果有分机号，中间用破折号分隔。</li></ul></li><li>邮政编码填充域常见验证点<ul><li>国内的邮政编码都是数字。</li><li>英国的邮编是字母与数字的组合，当我们测试国外邮编时需要先查一下该国的邮编格式。</li></ul></li><li>电子邮件填充域常见验证点<ul><li>电子邮件的格式为：<a href="mailto:xyz@xyz.xyz">xyz@xyz.xyz</a>，输入错误的格式比如：aa@aa，123，aa#aa.aa，检查是否有错误提示。</li><li>输入正确的电子邮件地址，需要能验证通过，并能收到相应的Email。</li></ul></li><li>购物数量填充域常见验证点<ul><li>在填写购物数量的地方，输入一个最大值，查看钱数累计的是否正确；</li><li>在填写购物数量的地方，输入一个负数，检查是否有正确的处理；</li><li>在填写购物数量的地方，输入一个数字0，检查是否有正确的处理；</li><li>在填写购物数量的地方，输入一个字母a，检查是否有正确的处理；</li><li>在填写购物数量的地方，输入一个特殊符号&gt;，检查页面能否正确显示。</li></ul></li><li>必填字段常见验证点<ul><li>必填字段不输入任何内容，直接提交，检查是否有错误提示；</li><li>必填字段只输入空格，然后提交，检查是否有错误提示；</li><li>必填字段的提示是否统一。</li></ul></li><li>单选按钮常见验证点<ul><li>一组单选按钮不能同时选中，只能选中一个；</li><li>逐一执行每个单选按钮的功能。分别选择了“男”、“女”后，保存到数据库的数据应该相应的分别为“男”、“女”；</li><li>一组执行同一功能的单选按钮在初始状态时必须有一个被默认选中，不能同时为空。</li></ul></li><li>复选框常见验证点<ul><li>多个复选框可以被同时选中（全选）；</li><li>多个复选框可以被部分选中（部分选）；</li><li>多个复选框可以都不被选中（全不选）；</li><li>逐一执行每个复选框的功能（选择保存后，查看保存结果是与所选择的一致）。</li></ul></li><li>大块文字区域常见验证点<ul><li>输入数据超出最大字符数，检查会出什么情况；</li><li>输入数据正好为最大字符数，检查是否能正确保存；</li><li>不输入任何数据，检查是否能正确保存；</li><li>在Firefox或Chrome浏览器下，大块文字区域右下角一般都可以拖放，尝试用鼠标进行拖放，检查会不会导致界面很难看或滚动条消失。</li></ul></li><li>下拉列表框常见验证点<ul><li>条目内容正确，无重复条目，无遗失条目；</li><li>逐一执行列表框中每个条目的功能。</li></ul></li><li>排序常见验证点<ul><li>选择正序排列后，再检查一下反序排序；</li><li>对文字的排序，检查是否按从A到Z的顺序；</li><li>对价格、数目等的排序，检查是否按数字大小顺序；</li><li>如果有分页，检查是否先好排序再分页。</li></ul></li><li>分页测试常见验证点：<ul><li>当没有数据时，首页、上一页、下一页、尾页标签全部置灰，不支持点击；</li><li>在首页时，“首页”“上一页”标签置灰；在尾页时，“下一页”“尾页”标签置灰；在中间页时，四个标签均可点击，且跳转正确；</li><li>翻页后，列表中的数据是否仍按照指定的顺序进行了排序；</li><li>各个分页标签是否在同一水平线上；</li><li>各个页面的分页标签样式是否一致；</li><li>分页的总页数及当前页数显示是否正确；</li><li>是否能正确跳转到指定的页数；</li><li>在分页处输入非数字的字符（英文、特殊字符等），输入0或超出总页数的数字，是否有友好提示信息；</li><li>是否支持回车键的监听。</li></ul></li><li>搜索框填充域常见验证点<ul><li>搜索按钮功能是否实现；</li><li>输入网站中存在的信息，能否正确搜索出结果；</li><li>输入键盘中所有特殊字符，是否报错；特别关注：_？＇＂# \ / – &lt; &gt;；特殊字符；</li><li>搜索出的结果页面是否与其他页面风格一致；</li><li>在输入域输入空格，点击搜索系统是否报错；</li><li>本站内搜索输入域中不输入任何内容，是否搜索出的是全部信息或者给予提示信息；</li><li>输入NULL/null、或很长的字符串，会出现什么结果；</li><li>精确查询还是模糊查询，如果是模糊查询输入：中%国（中*国），查询结果是不是都包含中国两个字的信息；</li><li>焦点放置搜索框中，搜索框默认内容是否自动被清空；</li><li>搜索输入域是否实现回车键监听事件。</li></ul></li><li>用户登录常见验证点<ul><li>用户名和密码都符合要求并且是正确的，检查是否能登录成功；</li><li>用户名和密码都不符合格式要求，检查是否能登录成功以及出错提示；</li><li>用户名符合要求，密码不符合要求，检查是否能登录成功以及出错提示；</li><li>密码符合要求，用户名不符合要求，检查是否能登录成功以及出错提示；</li><li>用户名或密码为空，检查是否能登录成功以及出错提示；</li><li>数据库中不存在的用户名，不存在的密码，检查是否能登录成功以及出错提示；</li><li>数据库中存在的用户名，错误的密码，检查是否能登录成功以及出错提示；</li><li>数据库中不存在的用户名，存在的密码，检查是否能登录成功以及出错提示；</li><li>输入的用户名或密码前存在空格，检查是否能登录成功以及出错提示；<br>按键盘上的回车键是否监听事件，能执行登录过程。</li></ul></li></ul></li></ul><h4 id="界面测试"><a href="#界面测试" class="headerlink" title="界面测试"></a>界面测试</h4><p>用户界面测试，英文是User interface testing，又称UI Testing，是指软件中的可见外观及其底层与用户交互的部分，包括菜单、对话框、窗口和其它控件。</p><ul><li>文字重叠问题</li><li>字符显示不完整</li><li>乱码</li><li>Tooltip对应不同</li><li>菜单对应不一致</li><li>鼠标变小手</li><li>不同浏览器显示问题</li><li>不同版本浏览器显示问题</li><li>不同OS &amp; Browser显示问题</li><li>对齐问题</li><li>图片显示问题</li><li>各个页面的样式风格是否统一 </li><li>各个页面的标题Title是否正确</li><li>导航处是否按相应的栏目级别显示</li><li>所有的图片是否都被正确装载，在不同的浏览器、分辨率下图片是否能正确显示（包括位置、大小）</li><li>窗口缩小</li><li>……</li></ul><h4 id="功能测试"><a href="#功能测试" class="headerlink" title="功能测试"></a>功能测试</h4><p>功能测试（Functional testing）。Web应用程序中的功能测试主要是对页面的链接、按钮等页面元素功能是否正常工作的测试。</p><ul><li>链接问题<ul><li>链接不工作</li><li>空链接</li><li>链接页面错误</li></ul></li><li>按钮问题<ul><li>按钮不工作</li><li>点击按钮产生JSerror</li></ul></li><li>本身应该具有的功能<ul><li>该有的功能没有实现</li><li>该有的功能错误对应到另一功能</li></ul></li><li>提示问题<ul><li>缺少提示</li><li>提示信息错误</li><li>提示导致UI问题</li></ul></li></ul><h4 id="安全测试"><a href="#安全测试" class="headerlink" title="安全测试"></a>安全测试</h4><ul><li><p>编辑框</p><pre><code class="html">&lt;script&gt;alert(“XSS”)&lt;/script&gt;</code></pre></li><li><p>URL</p><pre><code class="html">URL+&lt;script&gt;alert(“XSS”)&lt;/script&gt;</code></pre></li><li><p>修改URL</p><p>修改URL导致显示其他内容</p></li><li><p>SQL注入</p></li></ul><h3 id="写缺陷报告"><a href="#写缺陷报告" class="headerlink" title="写缺陷报告"></a>写缺陷报告</h3><p><img src="/images/image-20210527100653918.png" alt="image-20210527100653918"></p><h3 id="selenium写代码"><a href="#selenium写代码" class="headerlink" title="selenium写代码"></a>selenium写代码</h3><ul><li><p>Elements can be accessed in many ways:</p><ul><li><p>Element attribute: </p><ul><li><p>name</p><pre><code class="java">driver.findElement(By.name("password")).sendKeys(“aaaa");</code></pre></li><li><p>id</p><pre><code class="java">driver.findElement(By.id("user")).sendKeys("wangtianci");</code></pre></li><li><p>value</p></li></ul></li><li><p>Xpath </p><pre><code class="java">driver.findElement(By.xpath("//[@id=\"kw\"]")).sendKeys("selenium");</code></pre></li><li><p>LinkText</p><pre><code class="java">driver.findElement(By.linkText("学术")).click();</code></pre></li></ul></li><li><p>User actions that can be emulated:</p><ul><li>Click</li><li>Filling out text fields (sending keys)</li></ul></li></ul><pre><code class="java">import java.util.List;import org.openqa.selenium.By;import org.openqa.selenium.WebDriver;import org.openqa.selenium.chrome.ChromeDriver;import org.openqa.selenium.support.ui.Select;import org.openqa.selenium.WebElement;public class App {    private static WebDriver driver = new ChromeDriver();    public static void main(String[] args) throws Exception {        System.out.println("Hello, World!");        System.setProperty("webdriver.chrome.driver","/usr/bin/chromedriver");        //亚马逊        driver.get("https://www.amazon.com/?currency=USD&amp;language=en_US");        //选择搜索类别--书籍        selectItem("search-alias=stripbooks-intl-ship");        //搜索“selenium design patterns"        search("selenium design patterns");        //选择结果第一项        chooseResult();        //加入购物车并查看购物车        addToCart();            }    public static void sleep(int time) {        try {            Thread.sleep(time);        } catch (InterruptedException e) {            // TODO Auto-generated catch block            e.printStackTrace();        }    }    //选择搜索类别    public static void selectItem(String value){        sleep(2000);        WebElement formElement = driver.findElement        (By.id("searchDropdownBox"));        sleep(1000);        Select downList = new Select(formElement);        downList.selectByValue(value);        List&lt;WebElement&gt; list = downList.getAllSelectedOptions();        for(WebElement option : list) {            System.out.println(option.getText());        }        System.out.println(downList.getFirstSelectedOption().getText());    }    //搜索    public static void search(String keywords) {        sleep(1000);        WebElement searchBox = driver.findElement(By.id("twotabsearchtextbox"));        searchBox.sendKeys(keywords);        WebElement searchButton = driver.findElement(By.id("nav-search-submit-button"));        searchButton.click();        System.out.println("Searching...");    }    //选择搜索结果的第一项    public static void chooseResult() {        sleep(3000);        WebElement chosen = driver.findElement            (By.cssSelector(".a-link-normal:first-child"));        chosen.click();    }    //加入购物车    public static void addToCart() {        sleep(1500);        WebElement tag = driver.findElement(By.id("mediaTab_heading_1"));        tag.click();        sleep(1500);        WebElement toBuy = driver.findElement(By.id("a-autoid-2-announce"));        toBuy.click();        sleep(2000);        WebElement addToCart = driver.findElement(By.name("submit.addToCart"));        addToCart.click();        sleep(2000);        WebElement option_cart = driver.findElement(By.id("hlb-view-cart-announce"));        option_cart.click();    }}</code></pre><h2 id="白盒测试"><a href="#白盒测试" class="headerlink" title="白盒测试"></a>白盒测试</h2><h3 id="概念-2"><a href="#概念-2" class="headerlink" title="概念"></a>概念</h3><h4 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h4><p>白盒测试又叫结构测试，逻辑驱动测试或基于程序本身的测试。 </p><h4 id="动态测试"><a href="#动态测试" class="headerlink" title="动态测试"></a>动态测试</h4><h5 id="逻辑覆盖"><a href="#逻辑覆盖" class="headerlink" title="逻辑覆盖"></a>逻辑覆盖</h5><blockquote><p>每一个标准的含义 </p></blockquote><ul><li><p>语句覆盖 </p><p>设计若干条测试用例，使程序中每条可执行语句至少执行一次。</p></li><li><p>判定覆盖（分支覆盖） </p><p>设计测试用例，使程序中的每个逻辑判断的取真和取假的分支至少经历一次。</p></li><li><p>条件覆盖 </p><p>设计若干测试用例，使程序的每个判定中的每个条件的可能取值至少满足一次</p></li><li><p>判定-条件覆盖 </p><p>使判定中每个条件的可能取值至少满足一次，并且使每个判定分支至少执行一次。 </p><p>判定-条件覆盖能同时满足判定、条件两种覆盖标准。</p></li><li><p>条件组合覆盖 </p><p>设计用例，使得每个判断表达式中条件的各种可能组合都至少出现一次；</p></li><li><p>路径覆盖</p><p>设计足够多的测试用例，覆盖程序中的每条可能路径。</p></li></ul><pre class="mermaid">graph TDA[语句覆盖] --&gt; B[判定覆盖]A[语句覆盖] --&gt; C[条件覆盖]B[判定覆盖] --&gt; D[判定/条件覆盖]C[条件覆盖] --&gt; D[判定/条件覆盖]D--&gt;E[条件组合覆盖]--&gt;F[路径覆盖]</pre><h5 id="写覆盖"><a href="#写覆盖" class="headerlink" title="写覆盖"></a>写覆盖</h5><ul><li><p>Draw the CFG for the code fragment.</p></li><li><p>Compute the cyclomatic complexity number C, for the CFG.</p><ul><li>Cyclomatic complexity <ul><li>The number of ‘regions’ in the graph;  </li><li>The number of predicates + 1</li></ul></li></ul></li><li><p>Find at most C paths that cover the nodes and arcs in a CFG, also known as Basic Paths Set;</p></li><li><p>Design test cases to force execution along paths in the Basic Paths Set.</p></li></ul><h5 id="Junit写代码"><a href="#Junit写代码" class="headerlink" title="Junit写代码"></a>Junit写代码</h5><ul><li><p>The annotations in Junit</p><ul><li><p>@Test public void method()<br> this means the method is used to test the other methods.</p></li><li><p>@Before public void method()<br> the method is always executed before every test method is executed. It is usually used to prepare some data, or give some initial values.</p></li><li><p>@After public void method()<br> the method is always executed after every test method is executed. It is usually used to free up some storage space.</p></li><li><p>@BeforeClass public static void method()</p><p>the method is only executed once before all test methods are executed. It is usually used to do what all methods depend on, like connecting database.</p></li><li><p>@AfterClass public static void method()<br> contrary to @BeforeClass</p></li><li><p>@Ignore public void method()<br> the method will be ignored.</p></li><li><p>@Test(expected=IllegalArgumentException.class)<br>Check if the exception can be thrown</p><pre><code class="java">@Test(expected=ArithmeticException.class)public void testDiv() {    Math demoMath=new Math();    int expected;// expected result    int actualvalue;// actual result    expected=2;    actualvalue=demoMath.div(1, 0);    assertEquals(expected, actualvalue);}</code></pre></li><li><p>@Test(timeout=100)<br>If the execution time of the method is greater than 100ms, the test fails.</p><pre><code class="java">@Test(timeout=1)public void testDiv() {    Math demoMath=new Math();    int expected;//expected result    int actualvalue;//actual result    expected=2;    actualvalue=demoMath.div(2, 1);    assertEquals(expected, actualvalue);}</code></pre></li></ul></li><li><p>Assertion methods</p><ul><li>assertEquals</li><li>assertArrayEquals</li><li>assertFalse</li><li>assertTrue</li><li>assertNull</li><li>assertNotNull</li><li>assertSame</li><li>assertNotSame</li><li>…….</li></ul></li></ul><h2 id="性能测试"><a href="#性能测试" class="headerlink" title="性能测试"></a>性能测试</h2><h3 id="概念-3"><a href="#概念-3" class="headerlink" title="概念"></a>概念</h3><p>性能测试是通过自动化的测试工具模拟多种正常、峰值以及异常负载条件来对系统的各项性能指标进行测试。</p><p><img src="/images/st2.png" alt="st2"></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;&lt;img src=&quot;/images/st1.png&quot; alt=&quot;st1&quot;&gt;&lt;/p&gt;
&lt;h2 id=&quot;软件测试的概念&quot;&gt;&lt;a href=&quot;#软件测试的概念&quot; class=&quot;headerlink&quot; title=&quot;软件测试的概念&quot;&gt;&lt;/a&gt;软件测试的概念&lt;/h2&gt;&lt;h3 id=&quot;</summary>
      
    
    
    
    
    <category term="SE" scheme="http://example.com/tags/SE/"/>
    
  </entry>
  
  <entry>
    <title>C++ 11 &amp; C++14</title>
    <link href="http://example.com/2021/03/10/CplusplusScroll/"/>
    <id>http://example.com/2021/03/10/CplusplusScroll/</id>
    <published>2021-03-10T02:54:50.000Z</published>
    <updated>2022-03-11T13:20:02.290Z</updated>
    
    <content type="html"><![CDATA[<h2 id="语言可用性强化"><a href="#语言可用性强化" class="headerlink" title="语言可用性强化"></a>语言可用性强化</h2><h3 id="nullptr-与-constexpr"><a href="#nullptr-与-constexpr" class="headerlink" title="nullptr 与 constexpr"></a>nullptr 与 constexpr</h3><h3 id="类型推导"><a href="#类型推导" class="headerlink" title="类型推导"></a>类型推导</h3><p>在传统 C 和 C++中，参数的类型都必须明确定义，尤其是当我们面对一大堆复杂的模板类型时，必须明确的指出变量的类型才能进行后续的编码。</p><p>C++ 11 引入了 <code>auto</code> 和 <code>decltype</code> 这两个关键字实现了类型推导，让编译器来操心变量的类型。这使得 C++ 也具有了和其他现代编程语言一样，某种意义上提供了无需操心变量类型的使用习惯。</p><h4 id="auto"><a href="#auto" class="headerlink" title="auto"></a>auto</h4><p><code>auto</code> 在很早以前就已经进入了 C++，但是他始终作为一个存储类型的指示符存在，与 <code>register</code> 并存。</p><p>不使用auto书写迭代器:</p><pre><code class="c++">for(vector&lt;int&gt;::const_iterator itr = vec.cbegin(); itr != vec.cend(); ++itr)</code></pre><p>使用auto:</p><pre><code class="c++">for(auto itr = vec.cbegin(); itr != vec.cend(); ++itr);</code></pre><p>其它用法：</p><pre><code class="c++">auto i = 5;                        //i被推导为 intauto arr = new auto(10);        //arr被推导为 int *</code></pre><p>notice: <code>auto</code> 不可用于函数传参和推导数组类型，如以下错误：</p><pre><code class="c++">int add(auto x, auto y);{    auto i = 5;    int arr[10] = {0};    auto auto_arr = arr;    auto auto_arr2[10] = arr;}</code></pre><h4 id="decltype"><a href="#decltype" class="headerlink" title="decltype"></a>decltype</h4><p><code>decltype</code> 关键字是为了解决 auto 关键字只能对变量进行类型推导的缺陷而出现的，其用法和<code>sizeof</code> 相似：</p><pre><code class="c++">decltype(表达式);        //编译器分析表达式并得到其类型，却得不到其值//计算某表达式的值auto x = 1;auto y = 2;decltype(x + y) z;      //z为int型</code></pre><h4 id="尾返回类型、auto与decltype配合"><a href="#尾返回类型、auto与decltype配合" class="headerlink" title="尾返回类型、auto与decltype配合"></a>尾返回类型、auto与decltype配合</h4><p>以下写法不能通过编译：</p><pre><code class="c++">decltype(x + y) add(T x, U y);</code></pre><p>C++11引入尾返回类型（trailing return type),利用<code>auto</code> 将返回类型后置:</p><pre><code class="c++">template&lt;typename T, typename U&gt;auto add(T x, U y) -&gt; decltype(x + y) {    return x + y;}</code></pre><p>自C++14始，可直接让普通函数具备返回值推导，以下合法：</p><pre><code class="c++">template&lt;template T, typename U&gt;auto add(T x, U y) {    return x + y;}</code></pre><h3 id="区间迭代"><a href="#区间迭代" class="headerlink" title="区间迭代"></a>区间迭代</h3><p>区间迭代是指基于范围的 for 循环。</p><pre><code class="c++">int array[] = {1, 2, 3, 4, 5};for(auto &amp;x : array) {    std::cout &lt;&lt; x &lt;&lt; std::endl;}//std::vector的遍历//不使用区间迭代std::vector&lt;int&gt; arr(5, 100);for(std::vector&lt;int&gt;::iterator i = arr.begin(); i != arr.end(); ++i) {    std::cout&lt;&lt; *i &lt;&lt; std::endl;}//使用区间迭代//&amp;启用引用，若无则元素可读不可修改for(auto &amp;i : arr) {    std::cout&lt;&lt; i &lt;&lt; std::endl;}</code></pre><h3 id="初始化列表"><a href="#初始化列表" class="headerlink" title="初始化列表"></a>初始化列表</h3><p>在传统 C++ 中，不同的对象有着不同的初始化方法，例如普通数组、POD （plain old data，没有构造、析构和虚函数的类或结构体）类型都可以使用 <code>{}</code> 进行初始化，也就是我们所说的初始化列表。而对于类对象的初始化，要么需要通过拷贝构造、要么就需要使用 <code>()</code> 进行。这些不同方法都针对各自对象，不能通用。</p><pre><code class="c++">int arr[3] = {1,2,3};   // 列表初始化class Foo {    private:        int value;    public:        Foo(int) {}};Foo foo(1);             // 普通构造初始化</code></pre><p>在C++11中使用<code>std::initializer_list</code> 类型允许构造函数或其它函数像参数一样使用初始化列表，这就为类对象的初始化与普通数组和 POD 的初始化方法提供了统一的桥梁，如：</p><pre><code class="c++">#include &lt;initializer_list&gt;class Happiness {    public:        //初始化列表构造函数        Happiness(std::initializer_list&lt;int&gt; list) {}};Happiness happiness = {1, 2, 3, 4, 5};std::vector&lt;int&gt; v = {1, 2, 3, 4};//初始化列表作为普通函数的形参void func(std::initializer_list&lt;int&gt; list){    return;}func({1, 2, 3});//C++11提供统一语法以初始化任意对象 struct A {    int a;    float b;};struct B {    B(int _a, float _b): a(_a), b(_b) {}    private:        int a;        float b;};A a {1, 1.1};B b {2, 2.2};</code></pre><h3 id="模板增强"><a href="#模板增强" class="headerlink" title="模板增强"></a>模板增强</h3><h4 id="外部模板"><a href="#外部模板" class="headerlink" title="外部模板"></a>外部模板</h4><p>传统 C++ 中，模板只有在使用时才会被编译器实例化。换句话说，只要在每个编译单元（文件）中编译的代码中遇到了被完整定义的模板，都会实例化。这就产生了重复实例化而导致的编译时间的增加。并且，我们没有办法通知编译器不要触发模板实例化。</p><p>C++11 引入了外部模板，扩充了原来的强制编译器在特定位置实例化模板的语法，使得能够显式的告诉编译器何时进行模板的实例化：</p><pre><code class="c++">template class std::vector&lt;bool&gt;;            //强制实例化extern template class std::vector&lt;double&gt;   //不再该编译文件中实例化模板</code></pre><h4 id="尖括号”-gt-”"><a href="#尖括号”-gt-”" class="headerlink" title="尖括号”>”"></a>尖括号”&gt;”</h4><p>在传统 C++ 的编译器中，<code>&gt;&gt;</code>一律被当做右移运算符来进行处理，而自C++ 11 始，连续的右尖括号将变得合法，并且能够顺利通过编译:</p><pre><code class="c++">//嵌套模板std::vector&lt;std::vector&lt;int&gt;&gt; wow;</code></pre><h4 id="类型别名模板"><a href="#类型别名模板" class="headerlink" title="类型别名模板"></a>类型别名模板</h4><p>模板是用来产生类型的，模板不是类型。在传统 C++中，<code>typedef</code> 可以为类型定义一个新的名称，但是却没有办法为模板定义一个新的名称。</p><pre><code class="c++">template&lt; typename T, typename U, int value&gt;class SuckType {public:    T a;    U b;    SuckType():a(value),b(value){}};template&lt; typename U&gt;typedef SuckType&lt;std::vector&lt;int&gt;, U, 1&gt; NewType; // 不合法</code></pre><p>C++ 11 使用 <code>using</code> 引入了下面这种形式的写法，并且同时支持对传统 <code>typedef</code> 相同的功效：</p><pre><code class="c++">//通常我们使用 typedef 定义别名的语法是：typedef 原名称 新名称;//但是对函数指针等别名的定义语法却不相同，这通常给直接阅读造成了一定程度的困难。typedef int (*process)(void *);  // 定义了一个返回类型为 int，参数为 void* 的函数指针类型，名字叫做 processusing process = int(*)(void *); // 同上, 更加直观template &lt;typename T&gt;using NewType = SuckType&lt;int, T, 1&gt;;    // 合法</code></pre><h4 id="默认模板参数"><a href="#默认模板参数" class="headerlink" title="默认模板参数"></a>默认模板参数</h4><p>定义一个加法函数：</p><pre><code class="c++">template&lt;typename T, typename U&gt;auto add(T x, U y) -&gt; decltype(x+y) {    return x+y}</code></pre><p>要使用 add，就必须每次都指定其模板参数的类型。在 C++11 中提供了一种便利，可以指定模板的默认参数：</p><pre><code class="c++">template&lt;typename T = int, typename U = int&gt;auto add(T x, U y) -&gt; decltype(x+y) {    return x+y;}</code></pre><h4 id="变长参数模板"><a href="#变长参数模板" class="headerlink" title="变长参数模板"></a>变长参数模板</h4><p>模板一直是 C++ 所独有的黑魔法（<strong>Dark Magic</strong>）之一。在 C++11 之前，无论是类模板还是函数模板，都只能按其指定的样子，接受一组固定数量的模板参数；而 C++11 加入了新的表示方法，允许任意个数、任意类别的模板参数，同时也不需要在定义时将参数的个数固定。</p><pre><code class="c++">//任意个数、任意类别的模板参数template&lt;typename... Ts&gt; class Magic;</code></pre><p>模板类 Magic 的对象，能够接受不受限制个数的 typename 作为模板的形式参数:</p><pre><code class="c++">class Magic&lt;int,            std::vector&lt;int&gt;,            std::map&lt;std::string,                     std::vector&lt;int&gt;&gt;&gt; darkMagic;</code></pre><p>既然是任意形式，所以个数为 0 的模板参数也是可以的：<code>class Magic&lt;&gt; nothing;</code>，如果不希望产生的模板参数个数为 0，可以手动的定义至少一个模板参数：</p><pre><code class="c++">//手动定义至少一个模板参数template&lt;typename Require, typename... Args&gt; class Magic;</code></pre><p>变长参数模板也能被直接调整到到模板函数上：</p><pre><code class="c++">//传统 C 中的 printf 函数，虽然也能达成不定个数的形参的调用，但其并非类别安全//C++11 除了能定义类别安全的变长参数函数外，还可以使类似 printf 的函数能自然地处理非自带类别的对象//函数参数也使用 ... 代表不定长参数template&lt;typename... Args&gt; void printf(const std::string &amp;str, Args... args);</code></pre><p>如何对变长的模板参数进行解包：</p><pre><code class="c++">template&lt;typename... Args&gt;void magic(Args... args) {    std::cout &lt;&lt; sizeof...(args) &lt;&lt; std::endl;}//我们可以传递任意个参数给 magic 函数：magic();        // 输出0magic(1);       // 输出1magic(1, "");   // 输出2</code></pre><p>对参数进行解包，到目前为止还没有一种简单的方法能够处理参数包，但有两种经典的处理手法：</p><h5 id="1-递归模板函数"><a href="#1-递归模板函数" class="headerlink" title="1. 递归模板函数"></a>1. 递归模板函数</h5><p>递归是非常容易想到的一种手段，也是最经典的处理方法。这种方法不断递归的向函数传递模板参数，进而达到递归遍历所有模板参数的目的：</p><pre><code class="c++">#include &lt;iostream&gt;template&lt;typename T&gt;void printf(T value) {    std::cout &lt;&lt; value &lt;&lt; std::endl;}template&lt;typename T, typename... Args&gt;void printf(T value, Args... args) {    std::cout &lt;&lt; value &lt;&lt; std::endl;    printf(args...);}int main() {    printf(1, 2, "123", 1.1);    return 0;}</code></pre><h5 id="2-初始化列表展开"><a href="#2-初始化列表展开" class="headerlink" title="2. 初始化列表展开"></a>2. 初始化列表展开</h5><p>递归模板函数是一种标准的做法，但缺点显而易见的在于必须定义一个终止递归的函数。以下记录了使用初始化列表展开的方法（<strong>黑魔法</strong>）：</p><pre><code class="c++">// 编译这个代码需要开启 -std=c++14//通过初始化列表，(lambda 表达式, value)... 将会被展开。//由于逗号表达式的出现，首先会执行前面的 lambda 表达式，完成参数的输出。//唯一不美观的地方在于如果不使用 return 编译器会给出未使用的变量作为警告。template&lt;typename T, typename... Args&gt;auto print(T value, Args... args) {    std::cout &lt;&lt; value &lt;&lt; std::endl;    return std::initializer_list&lt;T&gt;{([&amp;] {        std::cout &lt;&lt; args &lt;&lt; std::endl;    }(), value)...};}int main() {    print(1, 2.1, "123");    return 0;}</code></pre><h3 id="面向对象增强"><a href="#面向对象增强" class="headerlink" title="面向对象增强"></a>面向对象增强</h3><h4 id="委托构造"><a href="#委托构造" class="headerlink" title="委托构造"></a>委托构造</h4><p>C++ 11 引入了委托构造的概念，这使得在同一个类中一个构造函数调用另一个构造函数，从而达到<strong>简化代码</strong>的目的：</p><pre><code class="c++">class Base {public:    int value1;    int value2;    Base() {        value1 = 1;    }    Base(int value) : Base() {  // 委托 Base() 构造函数        value2 = 2;    }};int main() {    Base b(2);    std::cout &lt;&lt; b.value1 &lt;&lt; std::endl;    std::cout &lt;&lt; b.value2 &lt;&lt; std::endl;}</code></pre><h4 id="继承构造"><a href="#继承构造" class="headerlink" title="继承构造"></a>继承构造</h4><p>在传统 C++ 中，构造函数如果需要继承是需要将参数一一传递的，这将导致效率低下。C++ 11 利用关键字 using 引入了继承构造函数的概念：</p><pre><code class="c++">class Base {public:    int value1;    int value2;    Base() {        value1 = 1;    }    Base(int value) : Base() {           // 委托 Base() 构造函数        value2 = 2;    }};class Subclass : public Base {public:    using Base::Base;                   // 继承构造};int main() {    Subclass s(3);    std::cout &lt;&lt; s.value1 &lt;&lt; std::endl;    std::cout &lt;&lt; s.value2 &lt;&lt; std::endl;}</code></pre><h4 id="显式虚函数重载"><a href="#显式虚函数重载" class="headerlink" title="显式虚函数重载"></a>显式虚函数重载</h4><p>在传统 C++ 中，经常容易发生意外重载虚函数的事情，如：</p><pre><code class="c++">struct Base {    virtual void foo();};struct SubClass: Base {    //可能是恰好加入的与基类中同名的函数或重载虚函数    void foo();};</code></pre><p>C++ 11 引入了 <code>override</code> 和 <code>final</code> 这两个关键字来防止上述情形的发生：</p><h5 id="override"><a href="#override" class="headerlink" title="override"></a>override</h5><p>当重载虚函数时，引入 <code>override</code> 关键字将显式的告知编译器进行重载，编译器将检查基函数是否存在这样的虚函数，否则将无法通过编译：</p><pre><code class="c++">struct Base {    virtual void foo(int);};struct SubClass: Base {    virtual void foo(int) override; // 合法    virtual void foo(float) override; // 非法, 父类没有此虚函数};</code></pre><h5 id="final"><a href="#final" class="headerlink" title="final"></a>final</h5><p><code>final</code> 则是为了防止类被继续继承以及终止虚函数继续重载引入的。</p><pre><code class="c++">struct Base {        virtual void foo() final;};struct SubClass1 final: Base {};                  // 合法struct SubClass2 : SubClass1 {};                  // 非法, SubClass 已 finalstruct SubClass3: Base {        void foo(); // 非法, foo 已 final};</code></pre><h5 id="显式禁用默认函数"><a href="#显式禁用默认函数" class="headerlink" title="显式禁用默认函数"></a>显式禁用默认函数</h5><p>在传统 C++ 中，如果程序员没有提供，编译器会默认为对象生成默认构造函数、复制构造、赋值算符以及析构函数。另外，C++ 也为所有类定义了诸如 <code>new</code> <code>delete</code> 这样的运算符。当程序员有需要时，可以重载这部分函数。</p><p>这就引发了一些需求：无法精确控制默认函数的生成行为。例如禁止类的拷贝时，必须将赋值构造函数与赋值算符声明为 <code>private</code>。尝试使用这些未定义的函数将导致编译或链接错误，则是一种非常不优雅的方式。</p><p>并且，编译器产生的默认构造函数与用户定义的构造函数无法同时存在。若用户定义了任何构造函数，编译器将不再生成默认构造函数，但有时候我们却希望同时拥有这两种构造函数，这就造成了尴尬。</p><p>C++11 提供了上述需求的解决方案，允许显式的声明采用或拒绝编译器自带的函数，如：</p><pre><code class="c++">class Magic {public:    Magic() = default;  // 显式声明使用编译器生成的构造    Magic&amp; operator=(const Magic&amp;) = delete; // 显式声明拒绝编译器生成构造    Magic(int magic_number);}</code></pre><h3 id="强类型枚举"><a href="#强类型枚举" class="headerlink" title="强类型枚举"></a>强类型枚举</h3><p>在传统 C++ 中，枚举类型并非类型安全，枚举类型会被视作整数，则会让两种完全不同的枚举类型可以进行直接的比较（虽然编译器给出了检查，但并非所有），<strong>甚至枚举类型的枚举值名字不能相同</strong>，这不是我们希望看到的结果。</p><p>C++ 11 引入了枚举类（enumaration class），并使用 <code>enum class</code> 的语法进行声明：</p><pre><code class="c++">enum class new_enum : unsigned int {    value1,    value2,    value3 = 100,    value4 = 100};</code></pre><p>这样定义的枚举实现了类型安全，首先他不能够被隐式的转换为整数，同时也不能够将其与整数数字进行比较，更不可能对不同的枚举类型的枚举值进行比较。但相同枚举值之间如果指定的值相同，那么可以进行比较：</p><pre><code class="c++">if (new_enum::value3 == new_enum::value4) {    // 会输出    std::cout &lt;&lt; "new_enum::value3 == new_enum::value4" &lt;&lt; std::endl;}</code></pre><p>在这个语法中，枚举类型后面使用了冒号及类型关键字来指定枚举中枚举值的类型，这使得我们能够为枚举赋值（未指定时将默认使用 int）。</p><p>而我们希望获得枚举值的值时，将必须显式的进行类型转换，不过我们可以通过重载 <code>&lt;&lt;</code> 这个算符来进行输出：</p><pre><code class="c++">#include &lt;iostream&gt;template&lt;typename T&gt;std::ostream&amp; operator&lt;&lt;(typename std::enable_if&lt;std::is_enum&lt;T&gt;::value, std::ostream&gt;::type&amp; stream, const T&amp; e){    return stream &lt;&lt; static_cast&lt;typename std::underlying_type&lt;T&gt;::type&gt;(e);}std::cout &lt;&lt; new_enum::value3 &lt;&lt; std::endl</code></pre><h2 id="语言运行期强化"><a href="#语言运行期强化" class="headerlink" title="语言运行期强化"></a>语言运行期强化</h2><h3 id="Lambda表达式"><a href="#Lambda表达式" class="headerlink" title="Lambda表达式"></a>Lambda表达式</h3><p>Lambda 表达式是 C++ 11 中最重要的新特性之一，而 Lambda 表达式，实际上就是提供了一个类似匿名函数的特性，而匿名函数则是在需要一个函数，但是又不想费力去命名一个函数的情况下去使用的。这样的场景其实有很多很多，所以匿名函数几乎是现代编程语言的标配。</p><h4 id="基础知识"><a href="#基础知识" class="headerlink" title="基础知识"></a>基础知识</h4><p>Lambda 表达式的基本语法如下：</p><pre><code class="c++">[捕获列表](参数列表) mutable(可选) 异常属性 -&gt; 返回类型 {    // 函数体}[ caputrue ] ( params ) opt -&gt; ret { body; };</code></pre><p>所谓捕获列表，其实可以理解为参数的一种类型，lambda 表达式内部函数体在默认情况下是不能够使用函数体外部的变量的，这时候捕获列表可以起到传递外部数据的作用。</p><p>根据传递的行为，捕获列表也分为以下几种：</p><h4 id="值捕获"><a href="#值捕获" class="headerlink" title="值捕获"></a>值捕获</h4><p>与参数传值类似，值捕获的前提是变量可以拷贝，不同之处则在于，被捕获的变量在 lambda 表达式被创建时拷贝，而非调用时才拷贝：</p><pre><code class="c++">void learn_lambda_func_1() {    int value_1 = 1;    auto copy_value_1 = [value_1] {        return value_1;    };    value_1 = 100;    auto stored_value_1 = copy_value_1();    // 这时, stored_value_1 == 1, 而 value_1 == 100.    // 因为 copy_value_1 在创建时就保存了一份 value_1 的拷贝    cout &lt;&lt; "value_1 = " &lt;&lt; value_1 &lt;&lt; endl;    cout &lt;&lt; "stored_value_1 = " &lt;&lt; stored_value_1 &lt;&lt; endl;}</code></pre><h4 id="引用捕获"><a href="#引用捕获" class="headerlink" title="引用捕获"></a>引用捕获</h4><p>与引用传参类似，引用捕获保存的是引用，值会发生变化。</p><pre><code class="c++">void learn_lambda_func_2() {    int value_2 = 1;    auto copy_value_2 = [&amp;value_2] {        return value_2;    };    value_2 = 100;    auto stored_value_2 = copy_value_2();    // 这时, stored_value_2 == 100, value_2 == 100.    // 因为 copy_value_2 保存的是引用    cout &lt;&lt; "value_2 = " &lt;&lt; value_2 &lt;&lt; endl;    cout &lt;&lt; "stored_value_2 = " &lt;&lt; stored_value_2 &lt;&lt; endl;}</code></pre><h4 id="隐式捕获"><a href="#隐式捕获" class="headerlink" title="隐式捕获"></a>隐式捕获</h4><p>手动书写捕获列表有时候是非常复杂的，这种机械性的工作可以交给编译器来处理，这时候可以在捕获列表中写一个 <code>&amp;</code> 或 <code>=</code> 向编译器声明采用引用捕获或者值捕获.</p><p>总结一下，捕获提供了 Lambda 表达式对外部值进行使用的功能，捕获列表的最常用的四种形式可以是：</p><ul><li><code>[]</code> 空捕获列表</li><li><code>[name1, name2, ...]</code> 捕获一系列变量</li><li><code>[&amp;]</code> 引用捕获, 让编译器自行推导捕获列表</li><li><code>[=]</code> 值捕获, 让编译器执行推导应用列表</li></ul><h4 id="表达式捕获（C-14）"><a href="#表达式捕获（C-14）" class="headerlink" title="表达式捕获（C++14）"></a>表达式捕获（C++14）</h4><p>值捕获、引用捕获都是已经在外层作用域声明的变量，因此这些捕获方式捕获的均为左值，而不能捕获右值。</p><p>C++14 允许捕获的成员用任意的表达式进行初始化，这就允许了右值的捕获，被声明的捕获变量类型会根据表达式进行判断，判断方式与使用 <code>auto</code> 本质上是相同的：</p><pre><code class="c++">#include &lt;iostream&gt;#include &lt;utility&gt;void learn_lambda_func_3(){    auto important = std::make_unique&lt;int&gt;(1);    auto add = [v1 = 1, v2 = std::move(important)](int x, int y) -&gt; int {        return x+y+v1+(*v2);    };    std::cout &lt;&lt; "add(3, 4) = " &lt;&lt; add(3, 4) &lt;&lt; std::endl;}</code></pre><p>在上面的代码中，<code>important</code> 是一个独占指针，是不能够被捕获到的，这时候我们需要将其转移为右值，在表达式中初始化。</p><h4 id="泛型Lambda-C-14"><a href="#泛型Lambda-C-14" class="headerlink" title="泛型Lambda(C++ 14)"></a>泛型Lambda(C++ 14)</h4><p>我们知道 <code>auto</code> 关键字不能够用在参数表里，这是因为这样的写法会与模板的功能产生冲突。但是 Lambda 表达式并不是普通函数，所以 Lambda 表达式并不能够模板化。这就为我们造成了一定程度上的麻烦：参数表不能够泛化，必须明确参数表类型。</p><p>然而自 C++14 始，Lambda 函数的形式参数可以使用 <code>auto</code> 关键字来产生意义上的泛型：</p><pre><code class="c++">void learn_lambda_func_4(){    auto generic = [](auto x, auto y) {        return x+y;    };    std::cout &lt;&lt; "generic(1,2) = " &lt;&lt; generic(1, 2) &lt;&lt; std::endl;    std::cout &lt;&lt; "generic(1.1,2.2) = " &lt;&lt; generic(1.1, 2.2) &lt;&lt; std::endl;}</code></pre><h3 id="函数对象包装器"><a href="#函数对象包装器" class="headerlink" title="函数对象包装器"></a>函数对象包装器</h3><h4 id="std-function"><a href="#std-function" class="headerlink" title="std::function"></a>std::function</h4><p>Lambda 表达式的本质是一个函数对象，当 Lambda 表达式的捕获列表为空时，Lambda 表达式还能够作为一个函数指针进行传递，例如：</p><pre><code class="c++">#include &lt;iostream&gt;using foo = void(int);  // 定义函数指针void functional(foo f) {    f(1);}int main() {    //Lambda表达式的捕获列表为空，可作为函数指针被传递    auto f = [](int value) {        std::cout &lt;&lt; value &lt;&lt; std::endl;    };    functional(f);  // 函数指针调用,f作为函数指针被传递    f(1);           // lambda 表达式调用    return 0;}</code></pre><p>上面的代码给出了两种不同的调用形式，一种是将 Lambda 作为函数指针传递进行调用，而另一种则是直接调用 Lambda 表达式，在 C++11 中，统一了这些概念，将能够被调用的对象的类型，统一称之为可调用类型。而这种类型，便是通过 <code>std::function</code> 引入的。</p><p>C++11 <code>std::function</code> 是一种通用、多态的<strong>函数封装</strong>，它的实例可以对任何可以调用的目标实体进行存储、复制和调用操作，它也是对 C++中现有的可调用实体的一种类型安全的包裹（相对来说，函数指针的调用不是类型安全的），换句话说，就是函数的容器。当我们有了函数的容器之后便能够更加方便的将函数、函数指针作为对象进行处理，如：</p><pre><code class="c++">#include &lt;functional&gt;#include &lt;iostream&gt;int foo(int para) {    return para;}int main() {    // std::function 包装了一个返回值为 int, 参数为 int 的函数    std::function&lt;int(int)&gt; func = foo;    int important = 10;    std::function&lt;int(int)&gt; func2 = [&amp;](int value) -&gt; int {        return 1+value+important;    };    std::cout &lt;&lt; func(10) &lt;&lt; std::endl;    std::cout &lt;&lt; func2(10) &lt;&lt; std::endl;}</code></pre><h4 id="std-bind-x2F-std-placeholder"><a href="#std-bind-x2F-std-placeholder" class="headerlink" title="std::bind/std::placeholder"></a>std::bind/std::placeholder</h4><p> <code>std::bind</code> 是用来绑定函数调用的参数的，它解决的需求是我们有时候可能并不一定能够一次性获得调用某个函数的全部参数，通过这个函数，我们可以将部分调用参数提前绑定到函数身上成为一个新的对象，然后在参数齐全后，完成调用，如：</p><pre><code class="c++">int foo(int a, int b, int c) {    ;}int main() {    // 将参数1,2绑定到函数 foo 上，但是使用 std::placeholders::_1 来对第一个参数进行占位    auto bindFoo = std::bind(foo, std::placeholders::_1, 1,2);    // 这时调用 bindFoo 时，只需要提供第一个参数即可    bindFoo(1);}</code></pre><blockquote><p><strong>notice：</strong>注意 <code>auto</code> 关键字的妙用。有时候我们可能不太熟悉一个函数的返回值类型，但是我们却可以通过 <code>auto</code> 的使用来规避这一问题的出现。</p></blockquote><h3 id="右值引用"><a href="#右值引用" class="headerlink" title="右值引用"></a>右值引用</h3><p>右值引用是 C++11 引入的与 Lambda 表达式齐名的重要特性之一。它的引入解决了 C++ 中大量的历史遗留问题，消除了诸如 <code>std::vector</code>、<code>std::string</code> 之类的额外开销，也才使得函数对象容器 <code>std::function</code> 成为了可能。</p><h2 id="新增容器"><a href="#新增容器" class="headerlink" title="新增容器"></a>新增容器</h2><h2 id="智能指针和引用计数"><a href="#智能指针和引用计数" class="headerlink" title="智能指针和引用计数"></a>智能指针和引用计数</h2><h2 id="正则表达式"><a href="#正则表达式" class="headerlink" title="正则表达式"></a>正则表达式</h2><h2 id="语言级线程支持"><a href="#语言级线程支持" class="headerlink" title="语言级线程支持"></a>语言级线程支持</h2><h2 id="其它"><a href="#其它" class="headerlink" title="其它"></a>其它</h2><h2 id="C-17"><a href="#C-17" class="headerlink" title="C++17"></a>C++17</h2>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;语言可用性强化&quot;&gt;&lt;a href=&quot;#语言可用性强化&quot; class=&quot;headerlink&quot; title=&quot;语言可用性强化&quot;&gt;&lt;/a&gt;语言可用性强化&lt;/h2&gt;&lt;h3 id=&quot;nullptr-与-constexpr&quot;&gt;&lt;a href=&quot;#nullptr-与-cons</summary>
      
    
    
    
    
    <category term="C++" scheme="http://example.com/tags/C/"/>
    
  </entry>
  
  <entry>
    <title>Hello World</title>
    <link href="http://example.com/1970/01/01/hello-world/"/>
    <id>http://example.com/1970/01/01/hello-world/</id>
    <published>1970-01-01T00:00:00.000Z</published>
    <updated>2022-03-09T03:36:36.885Z</updated>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><pre><code class="bash">$ hexo new "My New Post"</code></pre><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><pre><code class="bash">$ hexo server</code></pre><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><pre><code class="bash">$ hexo generate</code></pre><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><pre><code class="bash">$ hexo deploy</code></pre><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;Welcome to &lt;a href=&quot;https://hexo.io/&quot;&gt;Hexo&lt;/a&gt;! This is your very first post. Check &lt;a href=&quot;https://hexo.io/docs/&quot;&gt;documentation&lt;/a&gt; for</summary>
      
    
    
    
    
  </entry>
  
</feed>
